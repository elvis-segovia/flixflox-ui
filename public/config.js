// Placeholder for local dev. In the container this file is regenerated at
// startup by docker-entrypoint.sh from the deployment's environment variables.
// When empty, src/env.ts falls back to the build-time import.meta.env values.
window._env_ = {};
